# Backend API (Expo/portal uses this for file access)
# Web: localhost. Mobile: use admin web URL (proxies to backend) or direct backend
# Via admin web: http://YOUR_PC_IP:5173 (run: cd admin-dashboard && npm run dev)
# Direct: http://YOUR_PC_IP:3000
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
